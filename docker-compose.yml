version: '3.9'
services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.prod
    ports:
      - "8080:80"   # HTTP → redirects to HTTPS
      - "8443:443" # HTTPS
    restart: unless-stopped
