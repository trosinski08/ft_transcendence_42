Backend TODO (remaining items)

1. Implement authenticated endpoints for GDPR actions:
   - POST /api/auth/logout should invalidate the session token.
   - DELETE /api/users/me must remove the player record and cascade into related stats, queue entries, matches.

2. Harden database schema migrations:
   - Add foreign key constraints with ON DELETE CASCADE between players, queues, matches, and stats tables.
   - Ensure transactional safety for match updates touching multiple tables.

3. Sync stats persistence:
   - Expose endpoint to refresh leaderboard from historical matches.
   - Add background job (cron) to recompute ratings if match history changes.

4. Observability:
   - Forward Fastify request/response logs to ELK stack.
   - Add Grafana dashboard panels for backend latency and DB health once metrics endpoint is exposed.
